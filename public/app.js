'use script';
var learnjs = {};

learnjs.showView = function (hash) {
    var problemView = $('<div class="problem-view>"').text('Coming soon!');
    $('.vew-container').empty().append(problemView);
}

